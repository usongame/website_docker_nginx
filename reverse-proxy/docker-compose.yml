version: "3"
services:
  proxy:
    build: ./
    networks:
      - site1
      - site2
      - site_salestimes
    ports:
      - 80:80

networks:
  site1:
    external:
      name: site1_default
  site2:
    external:
      name: site2_default
  site_salestimes:
    external:
      name: site_salestimes_default
